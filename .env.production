# 프로덕션 환경 설정
NODE_ENV=production

# 앱 URL (프로덕션)
NEXT_PUBLIC_APP_URL=https://echoit.co.kr

# 프로덕션 환경에서는 JWT 만료 시간을 더 길게 설정
JWT_EXPIRY=24h
JWT_REFRESH_EXPIRY=30d

# 참고: 민감한 정보(DB 연결, API 키 등)는 서버 환경 변수로 직접 설정하거나
# 배포 환경의 비밀 관리 시스템을 통해 주입해야 합니다.