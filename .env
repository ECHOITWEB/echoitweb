# 앱 URL (개발 환경)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 포트 설정
PORT=3000

# 기본 환경
NODE_ENV=development

# MongoDB 설정 (실제 URI는 .env.local에 보관)
# MONGODB_URI=mongodb://localhost:27017/echoit
# MONGODB_DB=echoit

# TinyMCE 에디터 설정 (공개 설정만)
# 민감한 API 키는 .env.local에 보관
# NEXT_PUBLIC_TINYMCE_API_KEY=키는_env.local에_보관

# JWT 설정 (보안 키는 .env.local에 보관)
JWT_EXPIRY=12h
JWT_REFRESH_EXPIRY=7d
